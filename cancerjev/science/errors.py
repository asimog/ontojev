"""Scientific contract failures shared by deterministic lane methods."""


class ScienceError(Exception):
    def __init__(self, code: str, detail: str) -> None:
        super().__init__(f"{code}: {detail}")
        self.code = code
        self.detail = detail
