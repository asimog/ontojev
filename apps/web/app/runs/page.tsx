import { RunFeed } from "@/components/RunFeed";

export default function RunsPage() { return <><header className="page-title"><span className="eyebrow">CANONICAL HISTORY</span><h1>Research runs</h1><p>Every campaign, program cycle and researcher run is one durable event stream with immutable artifacts — readable across API, browser and research-process restarts.</p></header><RunFeed /></>; }

