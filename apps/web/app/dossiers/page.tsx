import { DossierArchive } from "@/components/DossierArchive";

export default function DossiersPage() { return <><header className="page-title"><span className="eyebrow">DERIVED RESEARCH OUTPUT</span><h1>Dossiers</h1><p>Authoritative JSON, indexed by SQLite; readable Markdown derives deterministically. Each dossier carries its run mode badge; fixture dossiers remain labeled synthetic.</p></header><DossierArchive /></>; }

